import React from "react";

function Logo({className}) {
    return (
        <div className={className}>
            <img src="./logo2.png" alt="logo" />
        </div>
    );
}

export default Logo;
